{
  "name": "backend-api",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "json-server --watch db.json --port 3001",
    "build": "json-server --watch db.json --port 3001",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^3.1.0"
  },
  "dependencies": {
    "json-server": "^0.17.0"
  }
}
